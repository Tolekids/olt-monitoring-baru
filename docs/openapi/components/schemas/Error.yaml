type: object
required: [message]
properties:
  message:
    type: string
  errors:
    type: array
    items:
      type: object
      required: [path, messages]
      properties:
        path:
          type: string
        messages:
          type: array
          items:
            type: string
